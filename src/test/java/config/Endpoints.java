package config;

public class Endpoints {

    // User endpoints
    public static final String USERS = "/users";
    public static final String USER_BY_ID = "/users/{userId}";

    // Post endpoints
    public static final String POSTS = "/posts";
    public static final String POST_BY_ID = "/posts/{postId}";

    // Comment endpoints
    public static final String COMMENTS = "/comments";
    public static final String COMMENTS_BY_ID = "/comments/{commentId}";

    // Album endpoints
    public static final String ALBUMS = "/albums";
    public static final String ALBUM_BY_ID = "/albums/{albumId}";

    // Photo endpoints
    public static final String PHOTOS = "/photos";
}